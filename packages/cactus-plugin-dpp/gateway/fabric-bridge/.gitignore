# These certs are regenerated by fabric-samples every time the test network is torn down
# and brought back up. They are committed here purely so that the gateway-3 docker
# container has something to mount for the demo; they are NOT production credentials.
